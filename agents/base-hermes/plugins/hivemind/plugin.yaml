name: hivemind
version: 0.1.0
description: "Native Hermes tools for hivemind sandboxed agents. Role gating registers the default fast path for each agent and keeps expensive scope simulation/source-inspection tools opt-in. All tools call back to the per-session bridge or bounded query-agent source mount. No MCP (slow)."
author: "hivemind-core"
